<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.administracionredes.administracionredesapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdministracionRedesApp">
        <activity android:name=".FallasFormActivity"></activity>
        <activity android:name=".FallasListaActivity" />
        <activity android:name=".LocalizacionFormActivity" />
        <activity android:name=".LocalizacionListaActivity" />
    </application>
</manifest>